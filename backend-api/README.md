# Backend API

Estrutura alvo do backend Express/Prisma em arquitetura modular.

O backend atual ainda roda na raiz do repositorio. Esta pasta documenta e prepara a separacao para monorepo/repo dedicado sem quebrar o deploy atual.
